[package]
name = "faction-system-tests"
version = "0.1.0"
edition = "2024"

[dependencies]
faction = { path = "../core" }
faction-protocol = { path = "../protocol" }

[dev-dependencies]
rstest = { workspace = true }
